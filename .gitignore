.DS_Store
/*.log
#docs
DOCs/
/DOCs
#production
/public/blog
/flow-typed

/node_modules/
/coverage/
/lib/
.env
# The /DOCs rule above also matches ./docs on case-insensitive filesystems (Windows/macOS).
# Agent + project documentation must stay tracked.
!docs/
!docs/**

# Backup files contain German teachers' PII (names, emails, phone numbers)
/backups/

# Claude Code / agent tooling: share commands, keep local overrides out.
.claude/settings.local.json
